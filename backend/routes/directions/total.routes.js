/*const express=require("express")
const router=express.Router()
const totalController=require("../../controllers/directions/total.controller")

router.route("/").get(totalController.getTotals)
                 .post(totalController.addTotal)

router.route("/:id").delete(totalController.deleteTotal)
                    .put(totalController.updateTotal)
                    .get(totalController.getTotal)

module.exports=router*/